export type SiteConfig = typeof siteConfig

export const siteConfig = {
  name: "Feature Table",
  description:
    "Feature table component with server side sorting, pagination, and filtering",
  mainNav: [],
  links: {},
}